diff --git a/pyproject.toml b/pyproject.toml
index f84317d..db5da41 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -1,6 +1,5 @@
 [build-system]
 requires = [
     "setuptools>=61.0",  
-    "torch>=2.5.1",
     ]
 build-backend = "setuptools.build_meta"
diff --git a/sam2/build_sam.py b/sam2/build_sam.py
index 3a3bef1..93aa546 100644
--- a/sam2/build_sam.py
+++ b/sam2/build_sam.py
@@ -91,7 +91,7 @@ def build_sam2(
     OmegaConf.resolve(cfg)
     model = instantiate(cfg.model, _recursive_=True)
     _load_checkpoint(model, ckpt_path)
-    model = model.to(device)
+    # model = model.to(device)
     if mode == "eval":
         model.eval()
     return model
diff --git a/setup.py b/setup.py
index 78a634c..bfd8e22 100644
--- a/setup.py
+++ b/setup.py
@@ -22,9 +22,9 @@ with open("README.md", "r", encoding="utf-8") as f:
 
 # Required dependencies
 REQUIRED_PACKAGES = [
-    "torch>=2.5.1",
-    "torchvision>=0.20.1",
-    "numpy>=1.24.4",
+    # "torch>=2.5.1",
+    # "torchvision>=0.20.1",
+    # "numpy>=1.24.4",
     "tqdm>=4.66.1",
     "hydra-core>=1.3.2",
     "iopath>=0.1.10",
